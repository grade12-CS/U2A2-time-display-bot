import becker.robots.*;

public abstract class TimeBot extends NumberBot{

    public TimeBot(City city, int x, int y, Direction direction, int things) {
        super(city, x, y, direction, things);
    }
}
